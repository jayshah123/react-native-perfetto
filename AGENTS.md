# AGENTS.md

Guidance for coding agents working on `react-native-perfetto`.

## Goal

This repository provides a React Native tracing library backed by a shared C++ core and TurboModule bridges on Android and iOS.
The public API is session-first (`startRecording` -> `TraceSession`) with first-class manual lifecycle helpers (`beginSection`/`endSection`) and deprecated compatibility wrappers.

## High-level architecture

- Public JS/TS API: `src/index.tsx`
- TurboModule spec: `src/NativePerfetto.ts`
- Shared tracer core: `cpp/ReactNativePerfettoTracer.h`, `cpp/ReactNativePerfettoTracer.cpp`
- Native C API surface: `cpp/include/rnperfetto/tracer.h`, `cpp/tracer_c_api.cpp`
- Android bridge:
  - Kotlin module: `android/src/main/java/com/perfetto/PerfettoModule.kt`
  - JNI bridge: `android/src/main/jni/reactnativeperfetto-jni.cpp`
  - CMake: `android/CMakeLists.txt`
- iOS bridge:
  - Module interface: `ios/Perfetto.h`
  - ObjC++ implementation: `ios/Perfetto.mm`
- App-owned JSI example workspace: `example-jsi/` (utility API surface, tracing hidden internally)

## Perfetto SDK behavior

- Full recording support requires vendored SDK files:
  - `cpp/third_party/perfetto/sdk/perfetto.h`
  - `cpp/third_party/perfetto/sdk/perfetto.cc`
  - Quick vendor command: `yarn vendor:perfetto-sdk`
- If SDK files are absent:
  - section/event/counter APIs still work through platform facilities.
  - `startRecording` and `TraceSession.stop()` intentionally return errors.

## Engineering rules

- Keep JS API backward compatible when possible.
- Keep `withSection`/`withRecording` as safe defaults, but treat manual `beginSection`/`endSection` as a first-class lane in docs and examples.
- Document and preserve strict LIFO section-end semantics (out-of-order section ends are ignored in JS to avoid JS/native stack desync).
- Keep Android and iOS behavior aligned for method semantics and errors.
- Prefer implementing core logic in C++ and keep platform bridges thin.
- Keep app-facing utility APIs domain-focused; do not expose raw tracing controls in `example-jsi` JS APIs.
- Do not add app-level New Architecture hooks that require host app manual edits.
- Keep C++ changes portable across Android and iOS.

## Big-picture integration paths to preserve

- Session-first RN tracing (`startRecording` -> `TraceSession`) remains the primary public lane.
- WebView bridge tracing remains protocol-driven and host-agnostic.
- App-owned utility JSI modules may consume tracer internals but should keep tracing hidden from app JS contracts.
- Native host integrations can use the C API without requiring JS API expansion.

## Adding new API methods

1. Update `src/NativePerfetto.ts` first.
2. Update JS wrapper in `src/index.tsx`.
3. Implement Android in `PerfettoModule.kt` and JNI if native needed.
4. Implement iOS in `Perfetto.mm`.
5. Implement/extend C++ core.
6. Update example app usage in `example/src/App.tsx`.
7. Document behavior in `README.md`.

## Build and validation commands

- Install dependencies: `yarn`
- Type check: `yarn typecheck`
- Lint: `yarn lint`
- Build JS artifacts: `yarn prepare`
- Run example Android: `yarn example android`
- Run example iOS: `yarn example ios`
- Install Maestro CLI: `yarn maestro:install`
- Run Maestro capture flow: `yarn maestro:test:capture-trace`

### Expanded E2E verification commands

- Capture legacy app trace: `yarn maestro:test:capture-trace:legacy`
- Capture WebView bridge trace: `yarn maestro:test:capture-webview-trace`
- Capture app-utilities trace: `yarn maestro:test:capture-app-utilities-trace`
- Pull latest Android trace: `yarn trace:pull:android`
- Pull latest iOS simulator trace: `yarn trace:pull:ios`
- Verify default trace slices: `yarn playwright:test:verify-trace`
- Verify busy-loop duration bounds: `yarn playwright:test:verify-busy-loop-1s`
- Verify WebView trace slices: `yarn playwright:test:verify-webview-trace`
- Verify app-utilities slices: `yarn playwright:test:verify-app-utilities-trace`

## Common pitfalls

- Mismatch between TS spec and platform method signatures will break codegen/build.
- C++ source location must remain under `cpp/` for Podspec + CMake wiring.
- iOS Promise methods must use `resolve`/`reject` signatures generated by codegen.
- Android JNI method names must match Kotlin `external` method signatures exactly.
- Android release builds are non-debuggable by default; `run-as` cannot access app cache traces in release mode.
- `example` and `example-jsi` share the `perfetto.example` app id; reinstall the intended app before running Maestro/trace pull flows.
